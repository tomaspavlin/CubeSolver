/**
 * Package defining neural network with it's evaluation and training using back propagation.
 * It does not work with actual cube but with Solve interface so that it is more portable and can be used not
 * only for rubic cube but for the
 * neural network computing XOR operation as well
 */
package com.tomas.cubesolver.neuralnetwork;